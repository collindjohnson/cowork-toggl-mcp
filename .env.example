# Toggl API Token — find it at https://track.toggl.com/profile
TOGGL_API_KEY=your_toggl_api_token_here

# Transport: "http" for Vercel/remote, "stdio" for local Claude Desktop
TRANSPORT=http

# Port (local dev only — Vercel sets this automatically)
PORT=3000
